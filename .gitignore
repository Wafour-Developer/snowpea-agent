# python
__pycache__/
*.pyc
.venv/
dist/
build/
*.egg-info/
# node
node_modules/
# tui/dist/snowpea-tui.js is tracked so `uv tool install git+…` can build the wheel
sdk/dist/
# omc runtime state (plans/specs are committed)
.omc/state/
.omc/logs/
.omc/notepad.md
# local
.snowpea/
.env

# keep the built TUI bundle so `uv tool install git+…` can build the wheel
!tui/dist/
!tui/dist/snowpea-tui.js
