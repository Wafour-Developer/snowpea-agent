/**
 * @snowpea/sdk — TypeScript client for the snowpea daemon.
 *
 * M0 exposes only the version constant; the protocol bindings are generated
 * into `src/protocol.ts` by `scripts/gen_protocol.py` at M1.
 */

export const SDK_VERSION = "0.1.0";

export default { SDK_VERSION };
