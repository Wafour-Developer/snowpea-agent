## Skills
Before you reply, scan the skills below. If one matches the task, or is even partially
relevant to it, you MUST load it with skill_view(name) and follow what it says. Err on the
side of loading: it is better to carry context you did not need than to miss a critical
step, a pitfall or an established workflow. A skill encodes how this kind of work is done
*here* — the conventions, the commands that actually work, the quality bar — so load one
even for a task you already know how to do. If a skill body in this conversation has been
replaced by a `[SKILL_PRUNED]` marker, its content was lost in a compaction: reload it with
skill_view before you act on anything that depends on it. Only proceed without loading a
skill if genuinely none of them are relevant. Run one as a command with /name.

<available_skills>
${SKILL_LINES}
</available_skills>
